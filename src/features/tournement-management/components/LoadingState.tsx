import React, { FunctionComponent } from 'react';

interface LoadingStateProps {}

const LoadingState: FunctionComponent<LoadingStateProps> = () => {
  return <div>Loading tournements ...</div>;
};

export default LoadingState;
